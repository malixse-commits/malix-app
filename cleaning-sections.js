(()=>{
 function init(){const root=document.querySelector('#cleaning');if(!root)return setTimeout(init,250);if(root.dataset.cleanSections)return;root.dataset.cleanSections='1';
  const mainTitle=root.querySelector('h2'),intro=root.querySelector('h2 + .note');
  if(!mainTitle||!intro)return;
  const hub=document.createElement('div');hub.id='cleanSectionHub';hub.innerHTML=`<div class="choice-grid" style="margin-top:18px"><button type="button" class="choice-card" data-clean-section="today"><span>🧹</span><strong>Idag hemma</strong><small>Dagens rum, dagens uppgifter och det du faktiskt gjorde.</small></button><button type="button" class="choice-card" data-clean-section="history"><span>📖</span><strong>Min städhistorik</strong><small>Idag, den här veckan, tidigare dagar och planeringen framåt.</small></button><button type="button" class="choice-card" data-clean-section="support"><span>🌿</span><strong>Städtips & städa i nuet</strong><small>Konkreta tips och tanken att göra en sak i taget och känna efter.</small></button></div>`;
  intro.insertAdjacentElement('afterend',hub);
  const back=document.createElement('button');back.type='button';back.className='back';back.id='cleanSectionBack';back.textContent='← Mitt hem';back.style.display='none';hub.insertAdjacentElement('afterend',back);
  const tips=[['💡 Fem minuter räknas','En liten insats är fortfarande gjord. Det behöver inte vara allt eller inget.'],['💡 Ett rum i taget','När dagens rum är gjort får det vara färdigt. Nästa rum har sin egen dag.'],['💡 Ett fönster räcker','Att tvätta fönster behöver inte betyda hela huset. Ett fönster idag räknas.'],['💡 Extra gjort räknas','Rensade du en låda, möblerade om eller gjorde något utöver planen? Skriv upp det också.'],['🌿 Se det som blev gjort','Lägg märke till det du faktiskt gjorde, inte bara det som finns kvar.']];let tipIndex=0;
  const historyTip=document.createElement('section');historyTip.id='cleanHistoryTip';historyTip.className='panel calm';historyTip.style.marginTop='18px';function renderTip(){const t=tips[tipIndex%tips.length];historyTip.innerHTML=`<h3>${t[0]}</h3><p>${t[1]}</p><button type="button" class="secondary" id="nextHistoryTip">Visa ett annat tips</button>`;historyTip.querySelector('#nextHistoryTip').onclick=()=>{tipIndex=(tipIndex+1)%tips.length;renderTip()}}renderTip();root.appendChild(historyTip);
  const support=document.createElement('section');support.id='cleanSupportSection';support.className='panel calm';support.style.marginTop='18px';support.innerHTML=`<h3>🌿 Städtips & städa i nuet</h3><p>Städning behöver inte betyda hela hemmet eller allt på listan. En yta eller en enda sak kan vara tillräckligt.</p><div class="panel"><h3>Städa i nuet</h3><p>Börja med en liten sak, till exempel att torka av diskbänken.</p><p>När den är klar: <strong>känn efter innan du fortsätter.</strong> Räcker det för idag? Orkar du en sak till? Vill du fortsätta en stund?</p><p>Poängen är inte att göra så mycket som möjligt. En sak är också ett resultat. Ibland upptäcker du däremot att du orkade mer än du trodde.</p></div><div class="panel" style="margin-top:14px"><h3>💡 Ett litet tips</h3><p id="supportTipText">${tips[0][1]}</p><button type="button" class="secondary" id="nextSupportTip">Visa ett annat tips</button></div><div class="panel" style="margin-top:14px"><h3>🧹 Städa i fyrkanter</h3><p>Vill du planera mer kan du dela upp städningen i mindre, hanterbara delar.</p><p><a class="primary" href="https://malix.se/22/stada-i-fyrkanter/13/11/08/" target="_blank" rel="noopener">Läs mer om Städa i fyrkanter →</a></p></div>`;root.appendChild(support);
  support.querySelector('#nextSupportTip').onclick=()=>{tipIndex=(tipIndex+1)%tips.length;support.querySelector('#supportTipText').textContent=tips[tipIndex][1]};
  function refs(){const today=root.querySelector('#cleanToday'),free=root.querySelector('#freeCleaningLog'),schedule=root.querySelector('#cleanSchedule')?.closest('section.panel'),add=root.querySelector('#addRoom')?.closest('section.panel'),history=root.querySelector('#cleanReflectionHistory')?.closest('section.panel');return{today,free,schedule,add,history}}
  function hideCourseTips(){root.querySelectorAll('#cleanToday section.panel').forEach(p=>{if(p.textContent.includes('Vill du ha mer stöd?')&&p.textContent.includes('Städa i fyrkanter'))p.style.display='none'})}
  function hideKnown(){const r=refs();[r.today,r.free,r.schedule,r.add,r.history,historyTip,support].filter(Boolean).forEach(x=>x.style.display='none');hideCourseTips()}
  function showHub(){hideKnown();hub.style.display='block';back.style.display='none';intro.style.display='block';window.scrollTo({top:root.offsetTop,behavior:'smooth'})}
  function open(which){const r=refs();hub.style.display='none';back.style.display='inline-block';intro.style.display='none';[r.today,r.free,r.schedule,r.add,r.history,historyTip,support].filter(Boolean).forEach(x=>x.style.display='none');if(which==='today'){if(r.today)r.today.style.display='block';if(r.free)r.free.style.display='block';hideCourseTips()}if(which==='history'){if(r.history)r.history.style.display='block';historyTip.style.display='block';if(r.schedule){r.schedule.style.display='block';const h=r.schedule.querySelector('h3');if(h)h.textContent='📅 Planera kommande städdagar'}if(r.add)r.add.style.display='block'}if(which==='support')support.style.display='block';window.scrollTo({top:root.offsetTop,behavior:'smooth'})}
  root.addEventListener('click',e=>{const b=e.target.closest('[data-clean-section]');if(b){e.preventDefault();open(b.dataset.cleanSection);return}if(e.target.closest('#cleanSectionBack')){e.preventDefault();showHub()}},true);
  setTimeout(showHub,500)
 }
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();