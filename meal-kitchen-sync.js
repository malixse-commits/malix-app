(() => {
  const KITCHEN_KEY='malix-smart-kitchen-v1';
  const norm=s=>String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9 ]/gi,' ').replace(/\s+/g,' ').trim();
  const aliases={'ostskiva':['ost'],'brödskiva':['bröd'],'rostat bröd':['bröd'],'knäckebröd':['knäckebröd'],'filmjölk':['filmjölk','fil'],'turkisk yoghurt':['turkisk yoghurt','yoghurt'],'müsli':['müsli'],'cornflakes':['cornflakes'],'havrefras':['havrefras'],'granola':['granola'],'ägg':['ägg'],'kvarg':['kvarg'],'yoghurt':['yoghurt'],'mjölk':['mjölk'],'kaffe':['kaffe'],'te':['te'],'banan':['banan'],'äpple':['äpple'],'päron':['päron'],'apelsin':['apelsin'],'bär':['bär'],'tomat':['tomat'],'gurka':['gurka'],'paprika':['paprika'],'sallad':['sallad'],'avokado':['avokado'],'rödlök':['rödlök']};
  function parseAmount(s){const m=String(s||'').toLowerCase().replace(',','.').match(/(\d+(?:\.\d+)?)\s*(kg|g|dl|ml|l|st|stycken|skiva|skivor|kopp|koppar|portion|portioner)?/);if(!m)return null;let n=Number(m[1]),u=m[2]||'';if(u==='kg'){n*=1000;u='g'}if(u==='l'){n*=1000;u='ml'}if(u==='dl'){n*=100;u='ml'}if(['stycken','skiva','skivor','portion','portioner'].includes(u))u='st';if(['kopp','koppar'].includes(u)){u='ml';n*=200}return {n,u}}
  function formatAmount(a){if(a.u==='ml'&&a.n>=1000)return `${Math.round(a.n/10)/100} l`;if(a.u==='g'&&a.n>=1000)return `${Math.round(a.n/10)/100} kg`;return `${Math.max(0,Math.round(a.n*100)/100)} ${a.u}`.trim()}
  function load(){try{const s=JSON.parse(localStorage.getItem(KITCHEN_KEY)||'{}');return {stock:Array.isArray(s.stock)?s.stock:[],shopping:Array.isArray(s.shopping)?s.shopping:[]}}catch{return {stock:[],shopping:[]}}}
  function save(s){localStorage.setItem(KITCHEN_KEY,JSON.stringify(s))}
  function matches(stockName,food){const sn=norm(stockName),fn=norm(food),candidates=aliases[fn]||[fn];return candidates.some(a=>sn.includes(norm(a))||norm(a).includes(sn))}
  function convert(food,have,used){if(have.u===used.u)return used;const f=norm(food);if(/kvarg|yoghurt|filmjolk|turkisk yoghurt/.test(f)){if(have.u==='g'&&used.u==='ml')return {n:used.n,u:'g'};if(have.u==='ml'&&used.u==='g')return {n:used.n,u:'ml'}}return null}
  function deduct(items){const st=load();if(!st.stock.length||!items.length)return;let changed=false;items.forEach(({food,quantity})=>{const stock=st.stock.find(x=>matches(x.item,food));if(!stock)return;const have=parseAmount(stock.amount),raw=parseAmount(quantity);if(!have||!raw)return;const used=convert(food,have,raw);if(!used)return;const remain=Math.max(0,have.n-used.n);stock.amount=formatAmount({n:remain,u:have.u});changed=true;if(remain<=0){st.shopping.push({id:Date.now()+Math.random(),item:stock.item,done:false,source:'Tog slut när måltiden loggades',category:'🥫 Skafferi & övrigt'});st.stock=st.stock.filter(x=>x.id!==stock.id)}});if(changed){save(st);window.malixRenderSmartKitchen?.()}}
  function parseMeal(foodText){return String(foodText||'').split(/,\s*/).map(part=>{const m=part.match(/^(.*?)\s*\((.*?)\)$/);return m?{food:m[1].trim(),quantity:m[2].trim()}:null}).filter(Boolean)}
  let beforeIds=new Set();
  document.addEventListener('submit',event=>{if(event.target?.id!=='mealForm')return;const meals=JSON.parse(localStorage.getItem('malix-meals')||'[]');beforeIds=new Set(meals.map(m=>`${m.date}|${m.meal}|${m.food}`));},true);
  document.addEventListener('malix-day-changed',()=>{const meals=JSON.parse(localStorage.getItem('malix-meals')||'[]');const newest=meals.find(m=>!beforeIds.has(`${m.date}|${m.meal}|${m.food}`));if(!newest)return;deduct(parseMeal(newest.food));beforeIds=new Set(meals.map(m=>`${m.date}|${m.meal}|${m.food}`));});
})();